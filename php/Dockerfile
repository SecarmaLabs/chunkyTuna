FROM php:5-apache
RUN apt-get update && apt-get -y install netcat net-tools vim
# this is for TUNNA
# RUN docker-php-ext-install sockets
# this is probably no longer necessary
# COPY custom.ini /usr/local/etc/php/conf.d/
